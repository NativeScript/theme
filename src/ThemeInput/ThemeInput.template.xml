<GridLayout rows="auto, auto" xmlns:ui="nativescript-theme-core">
    <!--<GridLayout row="1" columns="*, auto" class="input__validation" id="input__validation" tap="{{ showPopup }}">
        <label col="1" class="input__validation-shadow" />
        <StackLayout class="input__validation-text">
            <Label text="Remember, remember the fifth of November!" />
        </StackLayout>
        <label col="1" class="input__validation-arrow" />
    </GridLayout>-->
    <Label class="input__label" text="{{ label }}" visibility="{{ label !== undefined ? 'visible' : 'collapsed' }}" />
    <ui:ThemeIcon row="1" class="input__icon" name="{{ valid ? 'check-circle' : 'exclamation-circle' }}" tap="{{ showPopup }}" />
    <TextField row="1" class="input__field" />
<!--    visibility="{{ !valid ? 'visible' : 'collapsed' }}"-->
</GridLayout>
