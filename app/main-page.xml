<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo">
  <Page.actionBar>
      <ActionBar title="NativeScript Themes" icon="">
          <ActionBar.actionItems>
              <ActionItem icon="" text="Left" tap="" ios.position="left" />
              <ActionItem icon="" text="Right" tap="" ios.position="right" />
          </ActionBar.actionItems>
      </ActionBar>
  </Page.actionBar>
  <StackLayout class="main-page">
    <Button text="Basics" tap="{{ viewBasics }}" />
    <Button text="ListView" tap="{{ viewListView }}" />
    <Button text="SideDrawer" tap="{{ viewSideDrawer }}" />
    <Button text="Themes" tap="{{ viewThemes }}" />
    
  </StackLayout>
</Page>