<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo">
  <Page.actionBar>
    <ActionBar class="action-bar">
      <NavigationButton text="Back" icon="" tap="" />
      <StackLayout class="action-title">
        <Label text="Buttons"/>
      </StackLayout>
      <ActionBar.actionItems>
        <ActionItem ios.position="right">
          <ActionItem.actionView>
            <Button text="Right" class="action-item" />
          </ActionItem.actionView>
        </ActionItem>
      </ActionBar.actionItems>
    </ActionBar>
  </Page.actionBar>
  <StackLayout>
      <Button text="btn" tap="" class="btn" />
      <Button text="btn btn-primary" tap="" class="btn btn-primary" />
      <Button text="btn btn-outline" tap="" class="btn btn-primary btn-outline" />
      <Button text="btn isEnabled=false" tap="" isEnabled="false" class="btn btn-primary btn-disabled" />
  </StackLayout>
</Page>